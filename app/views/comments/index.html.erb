<h1>Comments on Article <%= link_to '#' + @article.id.to_s, article_path(@article) %></h1>

<h2>Rendered Table with Iteration</h2>
<table>
  <tr>
    <th>Commenter</th>
    <th>Comment</th>
  </tr>

  <% @article.comments.each do |comment| %>
    <tr>
      <td><%= comment.commenter %></td>
      <td><%= comment.body %></td>
    </tr>
  <% end %>
</table>

<h2>Rendered List with Partial</h2>
<%= render @article.comments %>